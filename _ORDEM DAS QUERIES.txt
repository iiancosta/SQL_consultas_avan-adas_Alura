SELECT DISTINCT TOP <NOME_DA_TABELA>.<NOME_DA_COLUNA>, <NOME_DA_TABELA>.* ou <LISTA DE CAMPOS>, 

(CASE WHEN <CONDIÇÃO> THEN <VALOR>
      WHEN <CONDIÇÃO> THEN <VALOR> 
      ELSE <VALOR> END) AS SOMETHING 

FROM <NOME DA TABELA1>
INNER JOIN <NOME DA TABELA2>
ON <NOME DA TABELA1>.<NOME DA KEY> = <NOME DA TABELA2>.<NOME DA KEY>
WHERE <FILTRO>
GROUP BY <LISTA DE CAMPOS>
HAVING <FILTRO ENVOLVENDO AGREGAÇÃO>
ORDER BY <LISTA DE CAMPOS> (ASC, DESC)
